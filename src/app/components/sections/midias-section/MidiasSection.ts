import styled from "styled-components";

export const StyledMidiasSection = styled.section`
  display: flex;
  flex-direction: column;
  align-items: left;
  gap: 1rem;
  color: var(--base-white);

  p {
    width: 55%;
  }

  section {
    display: flex;
    flex-direction: row;
    gap: 1rem;
  }
`;